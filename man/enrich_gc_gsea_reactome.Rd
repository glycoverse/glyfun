% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gc-gsea.R
\name{enrich_gc_gsea_reactome}
\alias{enrich_gc_gsea_reactome}
\title{Glycan-Centric Reactome Gene Set Enrichment Analysis}
\usage{
enrich_gc_gsea_reactome(
  dea_res,
  rank_by = "signed_log10p",
  aggr = "median",
  organism = "human",
  p_adj_method = "BH",
  p_cutoff = 0.05,
  min_gs_size = 10,
  max_gs_size = 500,
  exponent = 1,
  eps = 1e-10,
  seed = FALSE
)
}
\arguments{
\item{dea_res}{Differential analysis result. Can be one of:
\itemize{
\item Result from \code{\link[glystats:gly_limma]{glystats::gly_limma()}} (two groups), \code{\link[glystats:gly_ttest]{glystats::gly_ttest()}}, or \code{\link[glystats:gly_wilcox]{glystats::gly_wilcox()}},
called on an \code{\link[glyexp:experiment]{glyexp::experiment()}} of "traitproteomics" type.
\item A tibble with the following columns:
\itemize{
\item \code{protein}: Uniprot ID of proteins
\item \code{trait}: A glycosylation trait (e.g. "TFc" for proportion of core-fucosylated glycans)
\item \code{site}: The glycosylation site.
\item \code{p_val}: p-values, preferably adjusted p-values
\item \code{log2fc}: log2 of fold change
}
}}

\item{rank_by}{Criteria for ranking proteins.
One of the following:
\itemize{
\item "log2fc": log2 fold change with signs
\item "abs_log2fc": absolute log2 fold change
\item "log10p": negative log10 p-value
\item "signed_log10p" (default): log10 p-value with signs of log2 fold change
\item "log2fc_log10p": log2 fold change multiplied by negative log10 p-value
}}

\item{aggr}{Aggregation method for combining multiple scores across different traits and sites for the same protein.
One of "median", "mean", or "max". Defaults to "median".}

\item{organism}{Reactome organism name. Passed to \code{organism} of \code{\link[ReactomePA:gsePathway]{ReactomePA::gsePathway()}}.
One of "human", "rat", "mouse", "celegans", "yeast", "zebrafish", "fly". Defaults to "human".}

\item{p_adj_method}{P-value adjustment method.
One of "holm", "hochberg", "hommel", "bonferroni", "BH", "BY", "fdr", "none".
Passed to \code{pAdjustMethod} of downstream enrichment function.
Defaults to "BH".}

\item{p_cutoff}{P-value cutoff to filter significant terms.
Passed to \code{pvalueCutoff} of downstream enrichment function. Defaults to 0.05.}

\item{min_gs_size}{Minimal size of each gene set for analyzing.
Gene sets with fewer genes than this threshold will be excluded.
Passed to \code{minGSSize} of downstream enrichment function. Defaults to 10.}

\item{max_gs_size}{Maximum size of each gene set for analyzing.
Gene sets with more genes than this threshold will be excluded.
Passed to \code{maxGSSize} of downstream enrichment function. Defaults to 500.}

\item{exponent}{Weight of each step. Passed to \code{exponent} of \code{\link[clusterProfiler:gseGO]{clusterProfiler::gseGO()}}.
Defaults to 1.}

\item{eps}{Epsilon for calculating p-values. Passed to \code{eps} of \code{\link[clusterProfiler:gseGO]{clusterProfiler::gseGO()}}.
Defaults to 1e-10.}

\item{seed}{Logical indicating whether to set a random seed for reproducibility.
Passed to \code{seed} of \code{\link[clusterProfiler:gseGO]{clusterProfiler::gseGO()}}. Defaults to \code{FALSE}.}
}
\value{
A clusterProfiler \code{compareClusterResult} object.
It can be readily converted to a tibble with \code{\link[tibble:as_tibble]{tibble::as_tibble()}},
or visualized with \code{clusterProfiler} functions like \code{\link[clusterProfiler:reexports]{clusterProfiler::dotplot()}}.
}
\description{
Performs glycan-centric Reactome pathway Gene Set Enrichment Analysis (GSEA).
It ranks proteins within each glycan trait separately, then compares enriched
pathways across traits by running GSEA for each trait.
}
\section{What is glycan-centric enrichment?}{
In traditional glycoproteomics data analysis,
we usually perform differential expression analysis (DEA) on glycoforms,
extract proteins that have dysregulated glycosylation,
then perform functional enrichment (e.g. GO) on these proteins.
This is what \code{enrich_xxx()} functions do (e.g. \code{\link[=enrich_gsea_go]{enrich_gsea_go()}}).

\code{enrich_gc_xxx()} functions differ in that they link specific glycan traits with functional annotations.
Instead of answering the question
"Which functions are enriched in dysregulated glycoproteins?",
\code{enrich_gc_xxx()} answers questions like
"Which functions are enriched in proteins ranked highly for core-fucosylation changes?"
Higher specificity, deeper insights. By focusing on distinct glycan motifs,
it helps you pinpoint the functional relevance of specific glycosylation changes.
}

\section{How it ranks proteins}{
GSEA requires a ranked list of proteins as input.
This function first splits the DEA result by glycan trait, then ranks proteins
within each trait separately. For each trait, it applies the same ranking and
aggregation logic as \code{\link[=enrich_gsea_go]{enrich_gsea_go()}}, producing one ranked protein list per trait.
Those trait-specific ranked lists are then compared with
\code{\link[clusterProfiler:compareCluster]{clusterProfiler::compareCluster()}} using its formula interface.
}

\section{Common usage pattern}{
A common pattern of using this function is:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# 1. Use `glydet` to calculate derived traits or motif quantification.
trait_exp <- derive_traits(exp)  # or `quantify_motifs()`

# 2. Perform differential analysis with `glystats`.
dea_res <- gly_ttest(trait_exp)

# 3. Use this function.
go_res <- enrich_gc_gsea_go(dea_res)  # or other `enrich_gc_gsea_xxx()` functions
}\if{html}{\out{</div>}}
}

\seealso{
\code{\link[clusterProfiler:compareCluster]{clusterProfiler::compareCluster()}}, \code{\link[ReactomePA:gsePathway]{ReactomePA::gsePathway()}}
}
