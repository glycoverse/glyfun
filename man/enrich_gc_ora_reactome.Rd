% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gc-ora.R
\name{enrich_gc_ora_reactome}
\alias{enrich_gc_ora_reactome}
\title{Glycan-Centric Reactome Pathway Over Representation Analysis}
\usage{
enrich_gc_ora_reactome(
  dea_res,
  dea_p_cutoff = 0.05,
  dea_log2fc_cutoff = c(-1, 1),
  orgdb = "org.Hs.eg.db",
  organism = "human",
  universe = NULL,
  p_adj_method = "BH",
  p_cutoff = 0.05,
  q_cutoff = 0.2
)
}
\value{
A list with two elements:
\itemize{
\item \code{tidy_result}: A tibble with enrichment results containing the following columns:
\itemize{
\item \code{trait}: Glycan trait
\item \code{id}: Reactome pathway ID
\item \code{description}: Pathway description
\item \code{gene_ratio}: Ratio of genes in the pathway to total genes in the input
\item \code{bg_ratio}: Ratio of genes in the pathway to total genes in the background
\item \code{rich_factor}: Proportion of the pathway's total background genes found in the input
\item \code{fold_enrichment}: Ratio of \code{gene_ratio} to \code{bg_ratio} (magnitude of enrichment)
\item \code{z_score}: Directional trend of regulation (positive for up, negative for down)
\item \code{p_val}: Raw p-value from hypergeometric test
\item \code{p_adj}: Adjusted p-value
\item \code{q_val}: Q-value (FDR)
\item \code{gene_id}: Gene IDs in the pathway (separated by "/")
\item \code{count}: Number of genes in the pathway
}
\item \code{raw_result}: The raw clusterProfiler clusterProfResult object
The list has classes \code{glyfun_gc_ora_reactome_res}, \code{glyfun_gc_ora_res}, and \code{glyfun_res}.
}
}
\description{
Performs glycan-centric Reactome pathway Over-Representation Analysis (ORA).
Instead of traditional protein-centric enrichment, this function links specific
glycan traits to biological pathways. It helps answer questions like "Which
Reactome pathways are enriched in proteins with a specific dysregulated glycan motif?",
by grouping differential analysis results by glycan traits and computing
pathway enrichment for each trait.
}
\section{What is glycan-centric enrichment?}{
In traditional glycoproteomics data analysis,
we usually perform differential expression analysis (DEA) on glycoforms,
extract proteins that have dysregulated glycosylation,
then perform functional enrichment (e.g. GO) on these proteins.
This is what \code{enrich_xxx()} functions do (e.g. \code{\link[=enrich_ora_go]{enrich_ora_go()}}).

\code{enrich_gc_xxx()} functions differ in that they link specific glycan traits with functional annotations.
Instead of answering the question
"Which functions are enriched in dysregulated glycoproteins?",
\code{enrich_gc_xxx()} answers questions like
“Which functions are enriched in proteins with dysregulated core-fucosylation?”
Higher specificity, deeper insights. By focusing on distinct glycan motifs,
it helps you pinpoint the functional relevance of specific glycosylation changes.
}

\section{Common usage pattern}{
A common pattern of using this function is:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# 1. Use `glydet` to calculate derived traits or motif quantification.
trait_exp <- derive_traits(exp)  # or `quantify_motifs()`

# 2. Perform differential analysis with `glystats`.
dea_res <- gly_ttest(trait_exp)

# 3. Use this function.
go_res <- enrich_gc_ora_go(dea_res)  # or other `enrich_gc_xxx()` functions
}\if{html}{\out{</div>}}
}

\seealso{
\code{\link[clusterProfiler:compareCluster]{clusterProfiler::compareCluster()}}, \code{\link[ReactomePA:enrichPathway]{ReactomePA::enrichPathway()}}
}
